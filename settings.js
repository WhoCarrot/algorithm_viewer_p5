class Settings {
    constructor(element, amount, max, min, increment) {
        this.element = element;
        this.amount = amount;
        this.min = min;
        this.max = max;
        this.increment = increment;
    }
}